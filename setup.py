from setuptools import setup, find_packages

setup(
    name='languagegame',
    version='0.1.0', 
    description='Horizontally scaling LLM capabilities via Language Games.',
    packages=['languagegame']
)